DESTDIR=/tmp/opt/tivano
PYTHON_LIB=$(DESTDIR)/lib/python
INITDIR=$(DESTDIR)/etc/init.d
DAEMONDIR=$(DESTDIR)/usr/sbin
CONFDIR=$(DESTDIR)/etc/tivano
USER=root
GROUP=root
PIDFILE=/tmp/var/run/tivano.pid
LOGFILE=/tmp/var/log/tivano.log
ACCESS_LOG=/tmp/var/log/tivano-access.log

# Note: the makefile depends on wget which you can install
# or simply use curl rather than wget as curl
WGET=curl -k -O
